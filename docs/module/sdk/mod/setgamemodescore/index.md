---
title: SetGameModeScore
kind: function
longname: module:sdk.mod.SetGameModeScore
---

# SetGameModeScore

<SourceLink href="/source/sdk-d-ts/#L33102" label="sdk.d.ts:33102" />

<Signature code="SetGameModeScore(team: Team, newScore: number): void" />

**Parameters**

- `team` (Team)
- `newScore` (number)

**Returns**

- `void`

<Signature code="SetGameModeScore(player: Player, newScore: number): void" />

**Parameters**

- `player` (Player)
- `newScore` (number)

**Returns**

- `void`

<Signature
  code="SetGameModeScore(
	team: Team,
	newScore: number,
	gameStageType: GameStageType,
): void"
/>

**Parameters**

- `team` (Team)
- `newScore` (number)
- `gameStageType` ([GameStageType](/module/sdk/mod/gamestagetype))

**Returns**

- `void`

<Signature
  code="SetGameModeScore(
	player: Player,
	newScore: number,
	gameStageType: GameStageType,
): void"
/>

**Parameters**

- `player` (Player)
- `newScore` (number)
- `gameStageType` ([GameStageType](/module/sdk/mod/gamestagetype))

**Returns**

- `void`
