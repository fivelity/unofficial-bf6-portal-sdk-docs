---
title: SpotTarget
kind: function
longname: module:sdk.mod.SpotTarget
---

# SpotTarget

<SourceLink href="/source/sdk-d-ts/#L33260" label="sdk.d.ts:33260" />

<Signature
  code="SpotTarget(
	targetplayer: Player,
	duration: number,
	spotStatus: SpotStatus,
): void"
/>

**Parameters**

- `targetplayer` (Player)
- `duration` (number)
- `spotStatus` ([SpotStatus](/module/sdk/mod/spotstatus))

**Returns**

- `void`

<Signature
  code="SpotTarget(
	targetPlayer: Player,
	spotterPlayer: Player,
	duration: number,
	spotStatus: SpotStatus,
): void"
/>

**Parameters**

- `targetPlayer` (Player)
- `spotterPlayer` (Player)
- `duration` (number)
- `spotStatus` ([SpotStatus](/module/sdk/mod/spotstatus))

**Returns**

- `void`

<Signature code="SpotTarget(targetplayer: Player, spotStatus: SpotStatus): void" />

**Parameters**

- `targetplayer` (Player)
- `spotStatus` ([SpotStatus](/module/sdk/mod/spotstatus))

**Returns**

- `void`

<Signature
  code="SpotTarget(
	targetPlayer: Player,
	spotterPlayer: Player,
	duration: number,
): void"
/>

**Parameters**

- `targetPlayer` (Player)
- `spotterPlayer` (Player)
- `duration` (number)

**Returns**

- `void`

<Signature code="SpotTarget(targetplayer: Player, duration: number): void" />

**Parameters**

- `targetplayer` (Player)
- `duration` (number)

**Returns**

- `void`
