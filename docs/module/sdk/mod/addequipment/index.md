---
title: AddEquipment
kind: function
longname: module:sdk.mod.AddEquipment
---

# AddEquipment

<SourceLink href="/source/sdk-d-ts/#L33383" label="sdk.d.ts:33383" />

<Signature code="AddEquipment(player: Player, weapon: Weapons): void" />

**Parameters**

- `player` (Player)
- `weapon` ([Weapons](/module/sdk/mod/weapons))

**Returns**

- `void`

<Signature code="AddEquipment(player: Player, gadget: Gadgets): void" />

**Parameters**

- `player` (Player)
- `gadget` ([Gadgets](/module/sdk/mod/gadgets))

**Returns**

- `void`

<Signature
  code="AddEquipment(
	player: Player,
	weapon: Weapons,
	weaponPackage: WeaponPackage,
): void"
/>

**Parameters**

- `player` (Player)
- `weapon` ([Weapons](/module/sdk/mod/weapons))
- `weaponPackage` (WeaponPackage)

**Returns**

- `void`

<Signature
  code="AddEquipment(
	player: Player,
	arg1: Weapons,
	desiredInventorySlot: InventorySlots,
): void"
/>

**Parameters**

- `player` (Player)
- `arg1` ([Weapons](/module/sdk/mod/weapons))
- `desiredInventorySlot` ([InventorySlots](/module/sdk/mod/inventoryslots))

**Returns**

- `void`

<Signature
  code="AddEquipment(
	player: Player,
	gadget: Gadgets,
	desiredInventorySlot: InventorySlots,
): void"
/>

**Parameters**

- `player` (Player)
- `gadget` ([Gadgets](/module/sdk/mod/gadgets))
- `desiredInventorySlot` ([InventorySlots](/module/sdk/mod/inventoryslots))

**Returns**

- `void`

<Signature
  code="AddEquipment(
	player: Player,
	weapon: Weapons,
	weaponPackage: WeaponPackage,
	desiredInventorySlots: InventorySlots,
): void"
/>

**Parameters**

- `player` (Player)
- `weapon` ([Weapons](/module/sdk/mod/weapons))
- `weaponPackage` (WeaponPackage)
- `desiredInventorySlots` ([InventorySlots](/module/sdk/mod/inventoryslots))

**Returns**

- `void`

<Signature code="AddEquipment(player: Player, armor: ArmorTypes): void" />

**Parameters**

- `player` (Player)
- `armor` ([ArmorTypes](/module/sdk/mod/armortypes))

**Returns**

- `void`
