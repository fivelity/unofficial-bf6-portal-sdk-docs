---
title: ApplyAreaImpulseAndDamage
kind: function
longname: module:src/sdk.mod.ApplyAreaImpulseAndDamage
---

# ApplyAreaImpulseAndDamage

<SourceLink href="/source/sdk-d-ts/#L33135" label="sdk.d.ts:33135" />

<Signature
  code="ApplyAreaImpulseAndDamage(
	center: Vector,
	radius: number,
	impulseStrength: number,
	damageAmount: number,
): void"
/>

**Parameters**

- `center` (Vector)
- `radius` (number)
- `impulseStrength` (number)
- `damageAmount` (number)

**Returns**

- `void`

<Signature
  code="ApplyAreaImpulseAndDamage(
	center: Vector,
	radius: number,
	impulseStrength: number,
	damageAmount: number,
	impulseDirection: Vector,
): void"
/>

**Parameters**

- `center` (Vector)
- `radius` (number)
- `impulseStrength` (number)
- `damageAmount` (number)
- `impulseDirection` (Vector)

**Returns**

- `void`
